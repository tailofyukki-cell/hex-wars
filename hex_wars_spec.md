# HEX WARS（仮題）仕様書
## 大戦略ライク ターン制ウォー・シミュレーションゲーム

- 版数: v1.1（未決事項5点を確定・反映済み）
- 対象プラットフォーム: Windows / Linux（SDL2によりクロスプラットフォーム）
- 実装言語: C11
- 想定開発体制: 個人開発（Claude Code 併用）

---

## 1. コンセプト

### 1.1 ゲーム概要
ヘクスマップ上で陸・海・空のユニットを指揮し、都市や工場を占領して生産力を拡大しながら敵司令部（首都）の制圧を目指す、クラシックな「大戦略」系のターン制ウォー・シミュレーション。

- **世界観**: 架空WW2風。実在国・実在兵器名は使わず、架空の2大国「東方連邦」vs「西方同盟」（仮称）の大陸戦争を描く。ユニットは WW2 兵器をモチーフにした架空兵器（例: 中戦車「T-40型」風の独自名称）。実史の政治・思想的要素は扱わない。
- **アートスタイル**: フラットベクタ風。単色ベース+2〜3トーンの陰影、太めのアウトライン。ヘクスタイル・ユニットとも SVG で原版を作成し PNG 書き出し（48px 基準、ズーム用に 96px も書き出し）。

### 1.2 設計方針
- **データ駆動**: ユニット・地形・マップ・キャンペーンは全て外部テキストデータで定義。C側のコード変更なしにバランス調整可能。
- **静的メモリ主体**: ゲーム中の malloc/free を極力排除。上限数を定数で定義し固定長配列で管理（組み込み流儀）。起動時にアセット読込のみ動的確保を許容。
- **決定論的ロジック**: 乱数は seed 管理された自前 PRNG（xorshift 等）を使用し、リプレイ・デバッグ・テストを容易にする。
- **描画とロジックの分離**: ゲームロジックは SDL に依存しない純粋Cモジュールとし、単体テスト可能にする。

### 1.3 スコープ（フル仕様）
| 含む | 含まない（将来拡張） |
|---|---|
| 1人用キャンペーン（連続マップ・ユニット持越し） | オンライン対戦 |
| フリー対戦（1マップ、対CPU / ホットシート対人） | マップエディタGUI（テキスト編集で代替） |
| CPU思考ルーチン（難易度3段階） | Mod ローダー |
| 索敵（戦場の霧）ON/OFF | リアルタイム要素 |
| セーブ/ロード（任意タイミング） | |

---

## 2. 技術基盤

### 2.1 使用ライブラリ
| 用途 | ライブラリ | 備考 |
|---|---|---|
| ウィンドウ・描画・入力 | SDL2 | 2Dレンダラ（SDL_Renderer）使用。OpenGL直叩きはしない |
| 画像読込 | SDL2_image | PNG のみ使用 |
| フォント | SDL2_ttf | 日本語表示のため Noto Sans JP 等を同梱 |
| サウンド | SDL2_mixer | BGM: ogg / SE: wav |
| データ形式 | 自前パーサ | INI風の独自テキスト形式（3章参照）。JSONライブラリ依存を避ける |

### 2.2 ビルド
- CMake（最低 3.16）。`Debug` / `Release` 構成。
- コンパイラ: GCC / Clang / MSVC。`-Wall -Wextra -Werror`（Debug時）。
- C11、可変長配列（VLA）禁止、`goto` はエラー後始末のみ許可。

### 2.3 ディレクトリ構成
```
hexwars/
├── CMakeLists.txt
├── src/
│   ├── main.c            # エントリ・ゲームループ
│   ├── core/             # SDL非依存の純ロジック
│   │   ├── hex.c/.h      # ヘクス座標系
│   │   ├── map.c/.h      # マップデータ
│   │   ├── unit.c/.h     # ユニット状態
│   │   ├── rules.c/.h    # 移動・戦闘・補給の判定
│   │   ├── battle.c/.h   # 戦闘計算
│   │   ├── path.c/.h     # 経路探索
│   │   ├── ai.c/.h       # CPU思考
│   │   ├── campaign.c/.h # キャンペーン進行
│   │   ├── save.c/.h     # セーブ/ロード
│   │   └── rng.c/.h      # 決定論的乱数
│   ├── data/             # データ読込
│   │   └── parser.c/.h   # 独自テキスト形式パーサ
│   ├── ui/               # SDL依存層
│   │   ├── render.c/.h   # マップ・ユニット描画
│   │   ├── input.c/.h    # 入力→コマンド変換
│   │   ├── menu.c/.h     # メニュー・ダイアログ
│   │   ├── screen_*.c    # 各画面（タイトル/戦闘/結果 等）
│   │   └── assets.c/.h   # 画像・音・フォント管理
│   └── util/
├── assets/
│   ├── gfx/  sfx/  bgm/  font/
├── data/
│   ├── units.def         # ユニット定義
│   ├── terrain.def       # 地形定義
│   ├── maps/*.map        # マップ
│   └── campaign/*.cpn    # キャンペーン定義
└── tests/                # core層の単体テスト（SDL不要）
```

### 2.4 上限定数（静的確保の根拠）
```c
#define MAX_MAP_W        64
#define MAX_MAP_H        64
#define MAX_PLAYERS       2   /* 2陣営固定（対人・対CPUとも） */
#define MAX_UNITS       200   /* 全陣営合計・盤上 */
#define MAX_UNIT_TYPES   64
#define MAX_TERRAIN      16
#define MAX_CAMPAIGN_MAPS 20
#define MAX_CARRY_UNITS  30   /* キャンペーン持越し上限 */
```

---

## 3. データ形式（独自テキスト）

INI風のセクション+キー値。パーサは1パス、エラー時は行番号付きで報告。

```
# units.def の例
[unit INFANTRY]
name      = 歩兵
class     = LAND_FOOT        # 移動タイプ
cost      = 100
move      = 3
fuel      = 60
ammo      = 6
vision    = 2
atk_soft  = 55               # 対ソフト(歩兵等)攻撃力
atk_hard  = 15               # 対ハード(装甲)攻撃力
atk_air   = 5
atk_sea   = 0
def       = 30               # 防御力(地形補正前)
range_min = 1
range_max = 1
can_capture = 1              # 占領可能
transport_by = TRUCK,COPTER  # 搭載可能な輸送手段
```

マップファイルは「地形レイヤ（1文字=1ヘクス）」「建物・所有者」「初期配置ユニット」「勝利条件」「増援イベント」をセクションで持つ。

---

## 4. ヘクスグリッド仕様

### 4.1 座標系
- **表示**: フラットトップではなく**ポイントトップ（尖り上）ヘクス**、offset 配置（odd-r: 奇数行を右に半マスずらす）。大戦略系の見た目に一致。
- **内部演算**: axial 座標 `(q, r)` に変換して距離・近傍・探索を行う。格納は `tile[y][x]` の2次元固定配列（odd-r offset）。

```c
typedef struct { int8_t q, r; } Axial;
typedef struct { uint8_t x, y; } Cell;   /* offset 格納座標 */
```

- 近傍6方向はテーブル参照（奇数行/偶数行で2テーブル切替）。
- 距離: axial 変換後 `(|dq| + |dr| + |dq+dr|) / 2`。

### 4.2 描画パラメータ
- ヘクス1辺 = 24px 基準（タイル画像 48x48、縦方向オーバーラップあり）。
- ズーム: 1.0x / 1.5x / 2.0x の3段階（整数倍近傍縮小でドット絵の滲み回避）。
- スクロール: 画面端カーソル / マウス中ボタンドラッグ / 矢印キー。

---

## 5. ゲームルール

### 5.1 ターン構造
プレイヤー順（P1→P2→…）に手番が回る **IGO-UGO 方式**。1手番の内部フェイズ:

1. **収入フェイズ**: 所有する都市・工場・首都から資金収入。
2. **補給フェイズ**: 補給条件を満たすユニットの燃料・弾薬回復、都市上ユニットのHP回復（+2/最大10）。
3. **行動フェイズ**: 任意順でユニットの移動・攻撃・占領・搭載、および生産を実行。
4. **終了処理**: 燃料切れ判定（空中ユニットは墜落、海上は漂流→毎ターンHP-1）、勝利条件チェック。

### 5.2 ユニット
- HPは **1〜10**（大戦略式）。HPが攻撃力に比例（後述の戦闘式）。
- 各ユニットの状態:

```c
typedef struct {
    uint8_t  type;        /* units.def のインデックス */
    uint8_t  owner;
    Cell     pos;
    int8_t   hp;          /* 1..10, 0以下で撃破 */
    uint8_t  fuel;
    uint8_t  ammo;
    uint8_t  exp;         /* 0..100 経験値 */
    uint8_t  flags;       /* MOVED / ATTACKED / LOADED 等のビット */
    int8_t   cargo[2];    /* 搭載ユニットの index、-1で空 */
} Unit;
```

- **移動タイプ（class）**: `LAND_FOOT / LAND_WHEEL / LAND_TRACK / AIR / SEA / SEA_SUB` の6種。地形ごとの移動コストはマトリクスで定義。
- **経験値**: 撃破で+20、与ダメージで+2/HP。20ごとに1ランク（最大5ランク）、攻撃・防御に+3%/ランク。

### 5.3 地形（terrain.def）
| 地形 | 防御補正 | FOOT | WHEEL | TRACK | AIR | SEA | 備考 |
|---|---|---|---|---|---|---|---|
| 平地 | +0% | 1 | 1 | 1 | 1 | - | |
| 道路 | +0% | 1 | 0.5 | 0.5 | 1 | - | コストは内部で2倍整数管理 |
| 森 | +20% | 2 | 3 | 2 | 1 | - | 索敵時、隣接しないと視認不可 |
| 山 | +40% | 3 | - | - | 1 | - | FOOT のみ進入可 |
| 川 | +0% | 2 | - | - | 1 | - | |
| 海 | +0% | - | - | - | 1 | 1 | |
| 都市 | +30% | 1 | 1 | 1 | 1 | - | 収入+100、補給、占領対象 |
| 工場 | +30% | 1 | 1 | 1 | 1 | - | 収入+150、陸ユニット生産 |
| 空港 | +20% | 1 | 1 | 1 | 1 | - | 空ユニット生産・補給 |
| 港 | +20% | 1 | 1 | 1 | 1 | 1 | 海ユニット生産・補給 |
| 首都 | +40% | 1 | 1 | 1 | 1 | - | 陥落で敗北（条件による） |

※ 移動コストは内部では2倍した整数（道路=1, 平地=2…）で扱い浮動小数を排除。

### 5.4 移動
- 移動力の範囲内で Dijkstra により到達可能ヘクスをハイライト。
- **ZOC（支配地域）**: 敵地上ユニットの隣接6ヘクスはZOC。ZOCに進入した時点で移動終了（AIRは無視、FOOTは残移動1で1歩だけ抜け可）。
- 敵ユニットのいるヘクスは進入不可。味方ユニット上は通過可・停止不可。
- 索敵ONの場合、移動中に隠れていた敵を発見したらその手前で停止（アンブッシュ）。

### 5.5 戦闘
- 射程1のユニット同士は**攻撃→反撃**（反撃は生存者のみ、射程が届く場合のみ）。射程2以上の間接攻撃には反撃不可。
- ダメージ式（整数演算のみ）:

```
base   = ATK(対象カテゴリ別) × 攻撃側HP / 10
defmod = 100 + 地形防御% + 防御側経験ランク×3
atkmod = 100 + 攻撃側経験ランク×3
dmg_x100 = base × atkmod / defmod           # 期待ダメージ×100
dmg = (dmg_x100 + rand(-15%..+15%)) / 100 を 10段階HPへ丸め
最低保証: 攻撃有効(ATK>0)なら dmg >= 1
```

- **間接攻撃ユニット（range_min >= 2）は移動後攻撃不可**（FAM式）。同一ターンに「移動」か「攻撃」のいずれか一方のみ。units.def に `move_and_fire = 0/1` フラグを持たせ、直射ユニットは 1、間接ユニットは 0 とする（データ側で例外を作れる余地を残す）。
- 弾薬1消費/攻撃。弾薬0なら攻撃不可（反撃も不可）。
- 潜水艦は対潜能力のあるユニット以外から攻撃されない。

### 5.6 生産・資金
- 資金は陣営単位。工場/空港/港ヘクス上に**ユニットが不在**のとき、その施設で生産可能。生産ユニットはそのヘクスに「行動済み」で出現。
- 収入 = 首都300 + 都市100×n + 工場150×n（マップ側で倍率調整可）。
- 修理: 対応施設上で1ターンHP+2、コスト = ユニット価格×回復HP/10 ×0.2。

### 5.7 占領
- `can_capture=1` のユニットが建物ヘクス上で「占領」コマンド実行。建物耐久20をユニットHP分ずつ削り、0で所有権移転（大戦略/FAM折衷式）。占領途中でユニットが移動すると耐久リセット。

### 5.8 索敵（戦場の霧）
- **デフォルトON**。フリー対戦のセットアップ画面でのみOFF可（キャンペーンは常にON）。ONのとき、視界 = 各ユニット・所有建物の `vision` 合成。
- 森・都市の敵は隣接時のみ視認。潜水艦は対潜ユニット隣接時のみ。
- CPU側もプレイヤーと同一の視界情報のみで思考（チートなし。難易度HARDのみ視界+1の補正を許可）。

### 5.9 輸送
- 輸送ユニット（トラック・輸送ヘリ・輸送艦）は `cargo` に搭載。搭載中は攻撃不可。降ろすのは隣接する進入可能ヘクス。輸送ユニット撃破時、搭載ユニットも喪失。

### 5.10 勝利条件（マップ毎に定義）
- 敵首都占領 / 敵全滅 / 指定ターン内の拠点n個確保 / 指定ターン防衛。
- 敗北条件: 自首都陥落 / 全滅 / ターン切れ（防衛側でない場合）。

---

## 6. キャンペーンモード

### 6.1 構造
- `campaign/*.cpn` にマップ列と分岐を定義。ノードグラフ形式:

```
[node M01]
map      = maps/c01_border.map
next_win = M02
next_lose = GAMEOVER
carry    = 1        # ユニット持越し有効
bonus    = 500      # 勝利ボーナス資金
brief    = text/c01_brief.txt
```

- **分岐**: `next_win_fast = M03`（規定ターン以内勝利で別ルート）をサポート。
- **持越し**: 勝利時に生存ユニットのうち最大 `MAX_CARRY_UNITS` を次マップへ（HP・経験値・残燃料は全回復、経験値のみ維持）。次マップの初期配置エリアに展開。
- 資金も一部持越し（勝利時残額の50% + ボーナス）。

### 6.2 シナリオ・演出
- **シナリオ設定（架空WW2風）**: プレイヤーは「西方同盟」の一師団司令官。国境紛争（M01）→ 沿岸防衛 → 反攻上陸作戦 → 敵首都攻略、という10マップの大陸戦争を描く。分岐は「早期勝利で敵の防衛線構築前に進撃できる」形で表現。
- マップ間: ブリーフィング画面（1枚絵 + テキスト送り）。1枚絵もフラットベクタ風で統一。
- キャンペーン全体マップ（進行状況の可視化）はフェイズ2で実装（後述の開発計画参照）。

---

## 7. CPU思考ルーチン（AI）

### 7.1 全体構造
毎手番、以下の順で決定。全て core 層で完結し描画に依存しない。

1. **脅威マップ生成**: 全敵ユニットの攻撃到達範囲をヘクス毎に加算した `threat[y][x]`。
2. **戦略目標決定**: 占領候補（敵/中立建物）に価値スコア付け。首都>工場>都市。距離減衰あり。
3. **ユニット毎の行動**: 役割タグ（攻撃/占領/防衛/輸送/砲兵）を type から静的に割当て、役割別の評価関数で「移動先×行動」の全候補をスコアリングして最大を選択。
   - 攻撃評価 = 期待与ダメージ×敵価値 − 期待被反撃×自価値 + 地形防御ボーナス − threat ペナルティ
   - 占領評価 = 建物価値 / (距離+1) − threat
4. **生産**: 資金・戦況（自軍/敵軍の兵科構成比）から不足兵科を補充。序盤は歩兵・占領重視、中盤以降は装甲・航空へシフトするフェーズテーブル。

### 7.2 難易度
| 難易度 | 補正 |
|---|---|
| EASY | 評価に乱数±30%、収入×0.8、間接攻撃ユニットの運用簡略化 |
| NORMAL | 標準 |
| HARD | 収入×1.2、視界+1、退却判定（HP3以下で補給地点へ後退）有効 |

### 7.3 性能目標
- 64x64 / 200ユニットで CPU手番 1秒以内（一般的PC）。1ユニットあたり候補列挙は Dijkstra 1回 + 攻撃候補走査で O(マス数) に抑える。

---

## 8. UI / 画面遷移

### 8.1 画面一覧（状態機械）
```
TITLE → { CAMPAIGN_SELECT | FREE_BATTLE_SETUP | LOAD | OPTIONS }
CAMPAIGN_SELECT → BRIEFING → BATTLE → RESULT → (次ノード or TITLE)
FREE_BATTLE_SETUP → BATTLE → RESULT → TITLE
BATTLE 内サブ状態: IDLE / UNIT_SELECTED / MOVE_ANIM / TARGET_SELECT /
                   BATTLE_ANIM / MENU_OPEN / PRODUCTION / CPU_TURN
```
実装は `screen` 関数ポインタテーブル（`enter/update/draw/exit`）による明示的ステートマシン。

### 8.2 戦闘画面レイアウト
- 中央: マップビュー。カーソルヘクスをハイライト。
- 左下: 地形情報パネル（地形名・防御補正・所有者）。
- 右下: ユニット情報パネル（HP・燃料・弾薬・経験値バー）。
- 上部: ターン数 / 現手番陣営 / 資金。
- 戦闘アニメ: ウィンドウ内で双方ユニットのHPバー減少 + SE の簡易演出（設定でOFF可＝即時結果）。

### 8.3 操作（マウス+キーボード両対応）
| 操作 | マウス | キー |
|---|---|---|
| カーソル移動 | ポインタ | 矢印 |
| 決定/選択 | 左クリック | Z / Enter |
| キャンセル | 右クリック | X / Esc |
| ユニット情報 | ホバー | C |
| 次の未行動ユニット | - | N |
| ターン終了 | メニューから | E（確認ダイアログ） |

### 8.4 その他UI要件
- 全テキスト日本語。文字列はハードコードせず `data/text_ja.def` に集約（将来の多言語余地）。
- カラーユニバーサル配慮: 陣営色は色+ユニット枠形状で区別。

---

## 9. セーブ / ロード

- スロット式（10 + オートセーブ1）。手番開始時にオートセーブ。
- 形式: 独自バイナリ。ヘッダに `magic("HXWS")` / version / チェックサム(CRC32)。
- 内容: RNG state / ターン・手番 / 全 Unit 配列 / マップ所有権 / 資金 / キャンペーン進行 / 設定。
- 構造体を直接 fwrite せず、フィールド毎に little-endian でシリアライズ（パディング・移植性対策）。バージョン不一致は変換 or 拒否を明示。

---

## 10. サウンド

- BGM: タイトル / 戦闘（陣営毎に指定可） / 勝利 / 敗北。ループ再生。
- SE: カーソル / 決定 / キャンセル / 移動（兵科別3種） / 砲撃 / 爆発 / 占領 / ターン開始。
- 音量: BGM/SE 個別に0-10段階、オプションで永続化。

---

## 11. 初期コンテンツ量（目標）

| 項目 | 数 |
|---|---|
| ユニット種 | 24（陸14 / 空6 / 海4） |
| 地形種 | 12 |
| キャンペーンマップ | 10（分岐込み12ノード） |
| フリー対戦マップ | 6 |

ユニット例: 歩兵 / 対戦車歩兵 / 偵察車 / トラック / 戦車 / 重戦車 / 自走砲 / 対空車両 / 高射砲 / 補給車 / 戦闘機 / 爆撃機 / 攻撃ヘリ / 輸送ヘリ / 駆逐艦 / 潜水艦 / 輸送艦 / 巡洋艦 …

---

## 12. 開発フェーズ計画

### Phase 1: コア基盤（最初のマイルストーン）
- ヘクス座標系 + 単体テスト
- マップ表示・スクロール・カーソル
- ユニット表示・移動範囲計算・移動
- data パーサ + units.def / terrain.def / .map 読込

### Phase 2: 戦闘成立
- 戦闘計算 + 攻撃UI + 撃破
- ZOC / 占領 / 生産 / 資金
- ターン制御 / 勝利判定
- ホットシート対人戦（この時点で「遊べる」）

### Phase 3: CPU
- 脅威マップ / 役割別評価 / 生産AI
- 難易度3段階

### Phase 4: キャンペーン
- .cpn パーサ / ブリーフィング / 持越し / 分岐
- セーブ/ロード

### Phase 5: 仕上げ
- 索敵 / 輸送 / 経験値 / 修理
- 戦闘アニメ・サウンド・オプション
- バランス調整（キャンペーン10マップ通しプレイ）

各フェーズ完了時点でビルド・実行可能であることを維持する（縦切り開発）。

---

## 13. コーディング規約（抜粋）

- 命名: 型 `PascalCase` / 関数 `module_verb_noun()`（例 `hex_distance()`, `battle_calc_damage()`）/ 定数 `UPPER_SNAKE`。
- core 層は SDL ヘッダを include 禁止（CMake でレイヤ違反を検出）。
- 戦闘・移動などルール関数は「状態を引数で受け取り結果を返す」純関数寄りに書き、tests/ でゴールデンテストを持つ。
- assert は Debug のみ。Release ではエラーコード返却で継続。

---

## 14. 決定事項（v1.1 で確定・本文反映済み）

1. 間接攻撃ユニットは**移動後攻撃不可**（FAM式）→ 5.5 に `move_and_fire` フラグとして反映。
2. 索敵は**デフォルトON**（キャンペーンは常時ON、フリー対戦のみOFF可）→ 5.8 に反映。
3. アートスタイルは**フラットベクタ風**（SVG原版 → PNG 48px/96px 書き出し）→ 1.1 に反映。
4. シナリオは**架空WW2風**（西方同盟 vs 東方連邦の大陸戦争）→ 1.1 / 6.2 に反映。
5. プレイ人数は**2陣営固定** → `MAX_PLAYERS = 2` に反映（2.4）。
